# Windows Basics

**Platform:** TryHackMe
**Path:** Supplementary — completed to reinforce foundational knowledge
**Status:** Complete

---

## Key Notes

Windows file system navigation, key system directories (`C:\Windows\System32`, `C:\Temp` as common malware drop zone, `C:\Users\[user]\AppData` for persistence), essential CMD commands for analysts (ipconfig /all, netstat -ano, tasklist /svc, whoami /priv, net user). Alternate Data Streams detectable with `dir /r`.

---

## Placeholder

Detailed writeup can be expanded here.
